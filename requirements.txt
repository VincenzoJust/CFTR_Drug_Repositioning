# Core
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
scipy>=1.10

# Cheminformatics (GetMorganGenerator needs a recent RDKit)
rdkit>=2023.9

# Machine learning
scikit-learn>=1.3
xgboost>=2.0

# Bioactivity data sources
chembl-webresource-client>=0.10
# 3.0 changed the reader API: read_papyrus() now returns a polars LazyFrame and
# `chunksize` only toggles lazy mode -- it no longer yields chunks. The notebook
# targets that behaviour, so 2.x will not work.
papyrus-scripts>=3.0
polars>=1.0

# NOTE on conda environments: some conda-forge MKL builds (seen with
# mkl 2026.1.0) crash the interpreter on any numpy matrix multiply. If plain
# `numpy.random.rand(200,200) @ .T` kills your Python, switch the BLAS:
#     conda install -c conda-forge "libblas=*=*openblas"
